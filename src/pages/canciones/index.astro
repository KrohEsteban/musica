---
import Layout from '../../layouts/Layout.astro';

// Canciones creadas en Astro (usando el generador de tabs)
const cancionesAstro = [
  {
    nombre: "Last of Us - Main Theme",
    slug: "last-of-us",
    artista: "Videojuego",
    categoria: "videogames"
  }
];
---

<Layout title="🎵 Canciones de Ukelele">
  <h1>🎵 Canciones de Ukelele</h1>

  <hr />

  <p class="hint">
    Aquí encontrarás canciones creadas usando nuestro <a href="/musica/tabs/generador">generador de tabs</a>.
    Cada canción incluye tabs interactivas y explicaciones detalladas.
  </p>

  <br />

  <h2>🎵 Canciones Disponibles</h2>

  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1rem; margin: 1rem 0;">
    {cancionesAstro.map(cancion => (
      <div class="card">
        <h4>
          <a href={`/musica/canciones/${cancion.slug}`}>{cancion.nombre}</a>
        </h4>
      </div>
    ))}
  </div>

  <div class="card" style="margin-top: 2rem; text-align: center; background: #f8f9fa;">
    <h3>🚧 Más canciones próximamente</h3>
    <p>Estamos trabajando en agregar más canciones usando nuestro generador de tabs.</p>
    <p><a href="/musica/tabs/generador">¿Quieres crear tu propia tab?</a></p>
  </div>


</Layout>
